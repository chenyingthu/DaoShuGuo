physical:
  - name: "voltage_limits"
    description: "节点电压必须保持在允许范围内"
  - name: "reactive_bounds"
    description: "无功补偿量必须在设备边界内"
engineering:
  - name: "single_scenario_only"
    description: "当前 demo 仅验证默认工况"
  - name: "schema_tracked_outputs"
    description: "所有关键产物都必须按 schema 输出"
  - name: "pandapower_case33_only"
    description: "第一轮真实任务仅覆盖 pandapower case33bw"
research:
  - name: "baseline_required"
    description: "所有结论都必须与基线比较"
  - name: "claim_must_follow_taste_assessment"
    description: "报告主张必须受成果分级约束"
solver:
  baseline_vm_pu: 1.0
  candidate_vm_grid: [0.99, 1.0, 1.01, 1.02, 1.03, 1.04]
  experimental_vm_grid: [0.97, 0.98, 0.99]
  weak_bus_count: 5
  max_shunts: 2
  shunt_q_mvar_grid: [0.05, 0.1, 0.2, 0.3, 0.5]
  voltage_limits:
    min: 0.95
    max: 1.05
